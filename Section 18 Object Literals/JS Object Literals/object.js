// Property = key + value

// All keys are converted into a string, except symbols, which we havent covered from this point.
// For selecting a specific key in the object, we must accept that it is going to be a string. Whether it is a boolean, null, number.
