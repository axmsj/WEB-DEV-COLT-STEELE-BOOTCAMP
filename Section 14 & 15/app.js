// strings are indexed meaning each character has a corresponding index (positional number)

// some methods accept arguments that modify their behavior.
// Think of them as inputs that we can pass in.
// We pass these arguments inside of the parentheses
// ex thing.method(argument)

// Null
// Intentional absence if any value and must be assigned

// Undefined
// Variables that do not have an assigned value are undefined
